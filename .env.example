# ==============================================
# KEYLOR.FR - Site Vitrine (Variables d'environnement)
# ==============================================
# Ce fichier contient UNIQUEMENT les noms des variables.
# NE JAMAIS committer le vrai fichier .env avec les vraies valeurs !
# Copiez ce fichier vers .env et remplissez avec vos vraies valeurs.

# ==============================================
# DATABASE (PostgreSQL partagée avec l'intranet)
# ==============================================
# La vitrine utilise la MÊME base de données que l'intranet
# mais en MODE LECTURE SEULE (read-only)
DATABASE_URL=postgresql://user:password@host:port/database

# Détails de connexion PostgreSQL (si nécessaire)
PGHOST=your-postgres-host.com
PGPORT=5432
PGUSER=your-postgres-user
PGPASSWORD=your-postgres-password
PGDATABASE=your-database-name

# ==============================================
# OBJECT STORAGE (Images des biens immobiliers)
# ==============================================
# Bucket ID pour stocker les photos uploadées depuis l'intranet
DEFAULT_OBJECT_STORAGE_BUCKET_ID=your-bucket-id

# Chemins de recherche pour les objets publics et privés
PUBLIC_OBJECT_SEARCH_PATHS=/path/to/public/objects
PRIVATE_OBJECT_DIR=/path/to/private/objects

# ==============================================
# SÉCURITÉ - Secret partagé avec l'intranet
# ==============================================
# Ce secret permet à l'intranet d'uploader des photos
# dans l'Object Storage de keylor.fr
# ⚠️ DOIT ÊTRE IDENTIQUE dans l'intranet ET la vitrine
INTRANET_SHARED_SECRET=your-shared-secret-here

# ==============================================
# EMAIL (Resend ou Mailjet - pour notifications)
# ==============================================
# Pour envoyer des emails transactionnels (confirmations de rdv, etc.)
RESEND_API_KEY=your-resend-api-key

# OU utiliser Mailjet si vous préférez
MAILJET_API_KEY=your-mailjet-api-key
MAILJET_SECRET_KEY=your-mailjet-secret-key

# ==============================================
# GOOGLE MAPS (optionnel - pour la carte des biens)
# ==============================================
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# ==============================================
# OPENROUTESERVICE (optionnel - pour calcul d'itinéraires)
# ==============================================
OPENROUTESERVICE_API_KEY=your-openrouteservice-api-key

# ==============================================
# CONFIGURATION VPS (Déploiement autonome)
# ==============================================
# Si vous déployez sur votre propre VPS OVH, utilisez ces valeurs :

# DATABASE_URL=postgresql://keylor:VOTRE_MOT_DE_PASSE@localhost:5432/keylor_db
# PGHOST=localhost
# PGPORT=5432
# PGUSER=keylor
# PGPASSWORD=VOTRE_MOT_DE_PASSE
# PGDATABASE=keylor_db

# Object Storage local (sur le VPS)
# DEFAULT_OBJECT_STORAGE_BUCKET_ID=local
# PUBLIC_OBJECT_SEARCH_PATHS=/var/www/keylor/uploads/public
# PRIVATE_OBJECT_DIR=/var/www/keylor/uploads/private

# ==============================================
# NOTES IMPORTANTES
# ==============================================
# 1. DATABASE: Partagée avec l'intranet (même base)
# 2. OBJECT_STORAGE: Les images sont uploadées depuis l'intranet
# 3. INTRANET_SHARED_SECRET: DOIT être identique dans les 2 apps
# 4. La vitrine est en READ-ONLY sur la base de données
# 5. Pour VPS: Consultez GUIDE-DEPLOY-VPS.md pour la configuration complète
